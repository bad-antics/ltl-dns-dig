// ltl-dns-dig/main.ltl
//
// Recursive DNS resolver client (A, AAAA, MX, TXT, SRV).
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-dns-dig v0.1.0")
    io::println("Recursive DNS resolver client (A, AAAA, MX, TXT, SRV).")
    0
}
