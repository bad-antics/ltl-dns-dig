// examples/demo.ltl — runnable demo for ltl_dns_dig

import std::io
use ltl_dns_dig as lib

fn main() {
    io::println("ltl_dns_dig demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}